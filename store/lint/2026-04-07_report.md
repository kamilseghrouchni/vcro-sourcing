# Lint cycle report — 2026-04-07

First lint cycle on the post-wedge wiki. 219 entities scanned (61 cohorts, 66 institutions, 69 investigators, 23 platforms). The mechanical pre-pass at `2026-04-07_scan.json` produced 60 gaps, 57 staleness flags, 10 missing back-references, 30 unread cited PMIDs, and 4 consistency candidates. The four LLM stages then classified each finding into actions vs no-ops.

This is the orchestrator's queue. Each section ends with a recommended next action.

## Inputs

- Mechanical scan: `store/lint/2026-04-07_scan.json`
- Stage reports:
  - `store/lint/2026-04-07_gaps.md` (lint/gaps)
  - `store/lint/2026-04-07_consistency.md` (lint/consistency)
  - `store/lint/2026-04-07_staleness.md` (lint/staleness)
  - `store/lint/2026-04-07_connections.md` (lint/connections)

## Headline numbers

| Stage | Surfaced | Actionable | No-op | Deferred |
|---|---|---|---|---|
| Gaps (cohorts below depth 0.6) | 60 | 9 fillable + 5 single-source ingest | 16 structural | 30 (next cycle, 30-entity cap) |
| Consistency (same parent + modality) | 4 groups | 1 merge | 3 distinct | 0 conflicts |
| Staleness (age + single-source) | 57 | 0 stale URLs/pricing/contacts | 0 aged-but-stable | 30 single-source (route to gaps) |
| Connections — missing back-refs | 10 | **10 confirmed (zero false positives)** | 0 | 0 |
| Connections — unread cited PMIDs | 30 | 4 high-priority ingest | — | 13 medium + 13 low |

## Highest-priority actions (do first)

1. **Apply the 10 missing back-references via batched merge.** Five source entities (`adni-go2-plasma-lipidomics-aa`, `amyloid-biomarker-study-pooled`, `bioivt-healthy-plasma-sncrna`, `bioivt-mtb-plasma-sncrna`, `dian-obs-natural-history`) were added after the cycle 1 batched back-ref pass ran. Their `[[slug]]` body links to USC LONI, the Baker Institute platform, Guoyou Qin, ADNI-GO/2 plasma lipidomics, Willemijn Jansen, Pieter-Jelle Visser, Justin Gumas, PiB-PET amyloid, Eric McDade, and IRBLleida Biobank were never written into those targets' `referenced_by` lists. A single targeted batched re-pass on those 5 source entities resolves all 10 gaps. Idempotent, ~5 min wall.

2. **Merge `adni-go2-plasma-lipidomics-aa` into `adni-go2-plasma-lipidomics`.** The wedge cycle 2 created the AA arm as a separate entity, but it shares PMC12445873, the Baker Institute 781-lipid LC-MS/MS platform, and the same ADNI-GO/2 wave with its parent. The parent already contains an "African American sub-cohort" section linking to the AA slug — confirming the AA entity was always intended as an analytic stratification, not a distinct collection. The resolve granularity rule (different sample medium / assay platform / wave = separate) does not apply to ancestry stratification. After merge, the parent's depth recomputes; the AA entity is deleted.

3. **Recompile the top 3 fillable cohorts.** Run `compile/extract` with focused scope notes targeting the missing dimensions, then `compile/merge` the new fragments:
   - `pablo-de-olavide-healthy-amyloid-pet-controls` (depth 0.05 → ~0.30) from PMC11095469: 4 dimensions are quotably present in the source (collection_protocol_detail, eligibility_criteria, demographic_composition, confounders_and_exposures).
   - `cdc-national-als-biorepository-hair` (depth 0.14 → ~0.24) from PMC12444149: 2 dimensions (access_and_consent_scope, sponsor_and_funding).
   - `aspree-dementia-casecohort-lipidomics` (depth 0.14 → ~0.24) from PMC12269576: 2 dimensions (longitudinal_structure, demographic_composition) — the facts are already in the entity body but need to be moved into named dimension sections.

## Medium-priority actions

4. **Process the 6 other fillable gap entities** (gaps stage processed 30/60 due to the per-cycle cap; the next cycle picks up the remaining 30 first). Run gaps stage again on the next 30 lowest-depth cohorts.

5. **Single-source ingest expansion.** 5 cohorts are gap-flagged AND have only one source. The connections stage suggested 4 high-priority unread PMIDs as ingest candidates, all in the ALS lipid / clinical epidemiology cluster:
   - `PMID:36116464` (33×) — ALS/AD lipid dysregulation crossover
   - `PMID:11464847` (24×) — ALS foundational clinical/epidemiology
   - `PMID:21944778` (19×) — ALS lipid metabolism
   - `PMID:17023659` (19×) — ALS clinical reference
   - Recommended: a single search → pmc_convert → extract pass on these 4 PMIDs would deepen 5+ existing cohort entities and add 1-2 new institution/PI entities.

## Low-priority / deferred

6. **Format-deficient entities (~8).** A cluster including the AD Knowledge Portal brain bank cohorts and several ME/CFS/ALS single-site entities were written in narrative-flag format rather than standard dimension-section format. The scan correctly reports depth ≈ 0 because the coverage detector keys on dimension headers. These are content-adequate but format-deficient — a normalization re-merge with the SKILL's section template would fix them in one batch. Defer until the merge skill grows a "normalize body" mode.

7. **30 deferred staleness entries** (the lint/staleness 30-entity cap left 27 unprocessed). Most will turn out to be additional single-source noise; defer to next cycle.

8. **30 deferred gap entries** (same cap reasoning). Process in the next cycle.

9. **13 medium and 13 low priority unread PMIDs.** Reassess after the high-priority ingest finishes.

## What lint did NOT find

- **Zero hook rejections.** The 219 entity files all pass the schema hook, even ones with depth < 0.1.
- **Zero conflicting facts across entities.** The four consistency groups either merged cleanly or were correctly distinct.
- **Zero stale URLs or pricing claims that need re-verification.** Card.action URLs (LONI, DIAN, MVP) are all live institutional portals that the scanner did not flag with an age component.
- **Zero entity orphans.** Every cohort references at least one institution and one source PMC.

## Suggested next-cycle policy adjustments

- Raise the per-stage cap from 30 to 50 for the next cycle to clear the deferred entries faster.
- Add a "format-deficient" classifier to the gaps stage so the format vs content distinction becomes its own action class instead of being surfaced as low-depth noise.
- Add a body parse step to lint_scan.py that detects narrative-format entities and routes them to a separate "normalize" queue.
